<a class="wrap-img" ><img src="{{book.url}}"></a>
<h4> {{book.bookTitle}}</h4>
<h3>{{link-to  book.sectionTitle "books.show"  book (query-params bookInfoId=book.id ) }}</h3>

{{#if book.intro}}
  <div class="intro">
    {{book.intro}}
  </div>
{{/if}}
